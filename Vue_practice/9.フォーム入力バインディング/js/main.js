const app = Vue.createApp({
    data: () => ({
        message:"",
        // checked:false,
        // colors:[],
        // color:'',
        // selected:'',
        // selected:[],
        age:0

    }),

}).mount("#app")

// v-model
// 1.データバインディングで,要素のvalue属性を更新する. (v-bind)
// 2.イベントハンドリングで,受け取った値をデータに代入する.
